# Copy to .env. Endpoint and credential only — timeouts, retries and backoff are
# constants in cli.py, and every alignment parameter is a flag.

# Where the idref-resolver-api service runs.
IDREF_API_URL=http://localhost:8000

# Must match the API's IDREF_API_KEY. Leave empty if it runs without auth.
IDREF_API_KEY=
